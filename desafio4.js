let resultado 

function tipoTriangulo ( lado A, lado B, lado C) {
    if(lado A === lado B & lado B === lado C) {
        return "Equilátero";
    } else if ( ) {
        return "";
    } else {
        return "" ;
    }
}